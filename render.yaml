services:
  # ----------------------------------------
  # 1. Backend API (Node.js/Express)
  # ----------------------------------------
  - type: web
    name: ai-resource-backend
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      # These values must be filled out in the Render Dashboard securely
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: GOOGLE_CLOUD_PROJECT_ID
        sync: false
      - key: PORT
        value: 5000

  # ----------------------------------------
  # 2. Frontend App (Next.js)
  # ----------------------------------------
  - type: web
    name: ai-resource-frontend
    env: node
    plan: free
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      # This must be the live Render URL of your backend (e.g. https://ai-resource-backend.onrender.com)
      - key: NEXT_PUBLIC_API_URL
        sync: false
      
      # These values must be filled out in the Render Dashboard securely
      - key: NEXT_PUBLIC_SUPABASE_URL
        sync: false
      - key: NEXT_PUBLIC_SUPABASE_ANON_KEY
        sync: false
